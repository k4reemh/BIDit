{
  "name": "@bidit/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BIDit Manifest V3 extension — injects the auction panel onto Pump.fun streams.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@bidit/shared": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0"
  }
}
