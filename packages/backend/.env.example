# Copy to .env. For local dev/test the embedded-postgres harness uses this exact
# value; for a real deployment point it at your managed Postgres.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54329/bidit?connection_limit=20"
